version: "2"

run:
  timeout: 3m
  relative-path-mode: gomod

linters:
  default: standard
  enable:
    - gocritic
    - gosec
    - misspell
    - nilerr
    - noctx
    - unconvert
    - unparam
  settings:
    errcheck:
      check-type-assertions: true
      check-blank: true
    gocritic:
      enabled-tags:
        - diagnostic
        - performance
        - style
      disabled-checks:
        # Domain/application values are deliberately non-nullable. Passing
        # pointers solely to avoid copies would blur ownership and absence.
        - hugeParam
        - rangeValCopy
    govet:
      enable-all: true
      disable:
        - fieldalignment
        # Local err shadowing keeps transactional branches small and explicit.
        - shadow
    staticcheck:
      checks:
        - all

formatters:
  enable:
    - gofumpt
    - goimports
  settings:
    gofumpt:
      module-path: github.com/besartmorina/clinks/server
    goimports:
      local-prefixes:
        - github.com/besartmorina/clinks/server

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  exclusions:
    generated: strict
